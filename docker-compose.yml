services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: direcionador
      POSTGRES_PASSWORD: direcionador
      POSTGRES_DB: direcionador
    ports:
      - "55433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
